.env
.env.*
logs/
voices/
cv/*.pdf
cv/*.jpg
# Real personal data — never bake it into an image you might push to a public registry.
# (These are only excluded from git via .gitignore; Docker COPY ignores .gitignore.)
profile/profile.md
profile/projects.yaml
profile/knowledge.md
# /history conversation log (real user data — never bake it into an image).
data/
__pycache__/
**/__pycache__/
*.pyc
.git/
.github/
tests/
mcps/
scripts/
.venv/
venv/
